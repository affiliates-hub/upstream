import React from 'react';
import loader from './loader.module.css'

const Loader = () => {
    return (
        <div className={`${loader.conic} h-12 w-12 m-3 rounded-full`}>
           
        </div>
    );
};

export default Loader;